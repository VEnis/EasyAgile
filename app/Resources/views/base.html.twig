<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}EasyAgile{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets 'bootstrap/css/bootstrap.css' %}
                <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body style="padding-top: 50px;">
        {% block fullbody %}
            <div class="navbar navbar-inverse navbar-fixed-top">
                <div class="navbar-inner">
                    <div class="container">
                        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </a>
                        <a class="brand" href="{{ path('application_site_site_index') }}">EasyAgile</a>
                        <div class="nav-collapse collapse">
                            {{ knp_menu_render('ApplicationSiteBundle:Builder:mainMenu', {'currentClass': 'active'}) }}
                            <ul class="nav pull-right">
                                {% include '::userbar.html.twig' %}
                            </ul>

                        </div><!--/.nav-collapse -->
                    </div>
                </div>
            </div>

            <div class="container">
                {% block body %}
                {% endblock %}
                <hr>
                <footer>
                    <p>&copy; VEnis 2012</p>
                </footer>
            </div>
        {% endblock %}
        {% block javascripts %}
            {% javascripts
            'js/jquery-1.8.3.js'
            'bootstrap/js/bootstrap.js'
            %}
                <script type="text/javascript" src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}
    </body>
</html>
