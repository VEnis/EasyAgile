<h1>Session creation</h1>

<form action="{{ path('poker_session_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('poker_session') }}">
            Back to the list
        </a>
    </li>
</ul>
